Compile with <gcc *.c -lcrypto -o ctrtool.exe> (requires openssl)
Launch ./ctrtool.exe to check avaiblable commands
Put your nand backup in the same folder as "ctrnand.bin", as well as your boot9.bin dump.

ctrnand img partition is only available for old 3DS for now