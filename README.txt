Compile with <gcc *.c -lcrypto -o ctrtool.exe> (require openssl)
Launch ./ctrtool.exe for check avaiblable commands
put your nand backup on root with "ctrnand.bin" name, also put your boot9.bin dump.

ctrnand img partition is only available for old 3DS for now